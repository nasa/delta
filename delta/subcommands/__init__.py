"""
Subcommands called from the main `delta` executable.

Each subcommand contains a `setup_parser` function which
sets up the ArgParser to handle itself, setting the
appropriate main function and appropriate arguments for
when the subcommand is called.
"""
